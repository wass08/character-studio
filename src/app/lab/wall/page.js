import WallView from "@/components/lab/WallView";

export const metadata = {
  title: "Lab — wall · Character Studio",
  description: "Prototype of the live 3D character wall.",
};

export default function LabWallPage() {
  return <WallView />;
}
