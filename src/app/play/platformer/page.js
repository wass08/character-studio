import PlatformerView from "@/components/play/PlatformerView";

export const metadata = { title: "Platformer — Character Studio" };

export default function PlatformerPage() {
  return <PlatformerView />;
}
